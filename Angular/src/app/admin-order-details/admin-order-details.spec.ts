import { AdminOrderDetails } from './admin-order-details';

describe('AdminOrderDetails', () => {
  it('should create an instance', () => {
    expect(new AdminOrderDetails()).toBeTruthy();
  });
});
